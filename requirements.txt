# Regulatory Agent V2 — Dépendances
# Toutes sous licence permissive (MIT, Apache 2.0, BSD).
# Aucune dépendance OpenAI / Anthropic / Google — inférence 100 % locale.
#
# Audit 2026-09-01 : chaque ligne ci-dessous est utilisée par un import
# direct dans src/, scripts/, main.py ou tests/. Ne rien retirer sans
# grep préalable — plusieurs libs sont importées à l'intérieur de fonctions
# (lazy) et n'apparaissent pas dans un audit statique naïf.
#
# Les commentaires de justification sont sur leur PROPRE ligne : un
# commentaire en fin d'exigence empêchait `deptry` de parser la ligne, si
# bien que toutes les dépendances déclarées ici étaient vues comme
# transitives (49 faux positifs DEP003, constatés le 2026-09-16).

# --- Pile ML (Apple Silicon) — FIGÉE AU == ---
# transformers ⇄ huggingface-hub ⇄ tokenizers ⇄ safetensors ont des
# contraintes croisées trop serrées pour des plages : la moindre borne
# large casse la résolution (`pip-audit`). Versions installées ET validées
# sur m4pro2.
# - transformers 5.16.1 : au-dessus de PYSEC-2025-217 / PYSEC-2026-2288..2290
#   / CVE-2026-9856 (toutes corrigées en 5.x).
# - src/mlx_embedding.py : `from sentence_transformers import
#   SentenceTransformer` (backend d'embedding par défaut).
mlx==0.32.2
mlx-metal==0.32.2
mlx-lm==0.29.1
sentence-transformers==6.0.1
transformers==5.16.1
tokenizers==0.23.2
safetensors==0.8.0
torch==2.13.0
# huggingface-hub : tiré transitivement par transformers>=5 (exige >=1.0).
# Ne PAS le re-déclarer avec une borne — c'était la cause du conflit.

# NB : `mlx-embeddings` (voie MLX native "models/bge-m3-mlx") plantait
# `Stream(gpu, N)` sur Apple Silicon. Voie abandonnée — import lazy dans
# src/mlx_embedding.py, ImportError explicite si MODELE_EMBEDDING y est
# repointé.

# --- API HTTP ---
# fastapi : routing, DI, validation (src/api.py).
fastapi>=0.111,<1.0
# uvicorn : serveur ASGI, extra [standard] pour uvloop + httptools (main.py).
uvicorn[standard]>=0.29,<1.0
# jinja2 : Jinja2Templates pour la page / (src/api.py).
jinja2>=3.1,<4.0

# --- Modèles de données / configuration ---
# pydantic : validation + sérialisation (src/models.py, src/schemas.py).
pydantic>=2.7,<3.0
# pydantic-settings : BaseSettings, lecture du .env (config.py). C'est LUI qui
# lit `.env` : `python-dotenv` n'est jamais importé par le code, la dépendance
# a donc été retirée (audit 2026-09-16).
pydantic-settings>=2.3,<3.0

# --- Bases de données ---
# qdrant-client : recherche vectorielle (src/agents/retriever.py, scripts/ingest.py).
qdrant-client>=1.9,<2.0
# redis : files de validation humaine, rate-limit partagé (redis.asyncio).
redis>=5.0,<6.0
# asyncpg : pool PostgreSQL de l'audit chaîné (src/audit.py).
asyncpg>=0.29,<1.0

# --- HTTP client ---
# httpx : client sortant du Watcher (src/http_client.py) et TestClient.
httpx>=0.27,<1.0
# httpcore : protocole de backend réseau utilisé par src/http_client.py pour
# épingler l'adresse validée (SSRF). Déclaré explicitement : c'est un import
# DIRECT, pas un simple transit d'httpx.
httpcore>=1.0,<2.0

# --- Conversion PDF ---
# pdfplumber : détection des articles (scripts/pdf_parsing.py).
pdfplumber>=0.11,<1.0

# --- Tests ---
# pytest : suite de tests (tests/).
pytest>=9.0.3,<10.0
