# Regulatory Agent V2 — Dépendances runtime
# Toutes sous licence permissive (MIT, Apache 2.0, BSD).
# Aucune dépendance OpenAI / Anthropic / Google — inférence 100 % locale.
#
# Règle : une ligne = un import direct OU une contrainte d'épinglage assumée.
# Ne rien retirer sans grep préalable — plusieurs libs sont importées à
# l'intérieur de fonctions (lazy) et n'apparaissent pas dans un audit naïf.
#
# Les commentaires de justification sont sur leur PROPRE ligne : un
# commentaire en fin d'exigence empêche `deptry` de parser la ligne.
#
# Audit 2026-09-16 : la pile torch a été retirée (torch, sentence-transformers,
# scipy, scikit-learn, opencv, sympy, networkx ≈ 1 Go). L'embedding est
# désormais produit par la voie MLX native (`mlx-embeddings`), qui fonctionne
# une fois le modèle construit et utilisé sur le MÊME thread (cf.
# src/mlx_embedding.py). Les vecteurs ne sont PAS interchangeables entre les
# deux voies : l'index Qdrant a été reconstruit après la bascule.

# --- Pile ML (Apple Silicon) — FIGÉE AU == ---
# Versions installées ET validées sur m4pro2.
# mlx : noyau de calcul Apple Silicon.
mlx==0.32.2
# mlx-metal : backend GPU d'mlx (chargé dynamiquement, pas d'import).
mlx-metal==0.32.2
# mlx-lm : génération de texte (orchestrateur, Retriever, Explainer, Citation).
mlx-lm==0.29.1
# mlx-embeddings : embedding bge-m3 (voie native, remplace sentence-transformers).
mlx-embeddings==0.1.0
# mlx-vlm : dépendance DURE de mlx-embeddings (import au niveau module) ; non
# utilisée directement par le projet, mais indispensable au fonctionnement.
mlx-vlm==0.7.1
# transformers : EXIGÉ par mlx-lm (tokenizer, cf. mlx_lm/tokenizer_utils.py).
# Conservé bien que non importé directement : sans lui, `import mlx_lm` échoue.
transformers==5.16.1
# tokenizers : backend rapide du tokenizer, épinglé avec transformers.
tokenizers==0.23.2
# safetensors : format des poids (modèle local models/bge-m3-mlx).
safetensors==0.8.0

# --- API HTTP ---
# fastapi : routing, DI, validation (src/api.py).
fastapi>=0.111,<1.0
# uvicorn : serveur ASGI, extra [standard] pour uvloop + httptools (main.py).
uvicorn[standard]>=0.29,<1.0
# jinja2 : Jinja2Templates pour la page / (src/api.py) — également exigé par mlx-lm.
jinja2>=3.1,<4.0

# --- Modèles de données / configuration ---
# pydantic : validation + sérialisation (src/models.py, src/schemas.py).
pydantic>=2.7,<3.0
# pydantic-settings : BaseSettings, lecture du .env (config.py). C'est LUI qui
# lit `.env` : `python-dotenv` n'est jamais importé et a été retiré.
pydantic-settings>=2.3,<3.0

# --- Bases de données ---
# qdrant-client : recherche vectorielle (src/agents/retriever.py, scripts/ingest.py).
qdrant-client>=1.9,<2.0
# NB : la bibliothèque `redis` n'est plus déclarée. Le client RESP est écrit à
# la main dans src/redis_client.py (sockets + asyncio.to_thread) ; la
# réintroduire donnerait deux clients concurrents pour les mêmes files HITL et
# le même compteur de rate-limit.
# asyncpg : pool PostgreSQL de l'audit chaîné (src/audit.py).
asyncpg>=0.29,<1.0

# --- HTTP client ---
# httpx : client sortant du Watcher (src/http_client.py) et TestClient.
httpx>=0.27,<1.0
# httpx2 : client du TestClient de starlette >= 1.x. Depuis starlette 1.6,
# utiliser `httpx` avec `starlette.testclient` est DÉPRÉCIÉ et émet un
# StarletteDeprecationWarning à chaque session de tests — la prochaine version
# majeure de starlette ne le supportera plus. Installé pour les tests
# uniquement, mais déclaré ici : c'est le même environnement, et la CI installe
# `requirements.txt` avant de lancer pytest.
httpx2>=2.13,<3.0
# httpcore : protocole de backend réseau utilisé par src/http_client.py pour
# épingler l'adresse validée (SSRF). Import DIRECT, pas un transit d'httpx.
httpcore>=1.0,<2.0

# --- Conversion PDF ---
# pdfplumber : détection des articles (scripts/pdf_parsing.py).
pdfplumber>=0.11,<1.0

# --- Tests ---
# pytest : suite de tests (tests/).
pytest>=9.0.3,<10.0
