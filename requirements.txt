# Regulatory Agent V2 — Dépendances
# Toutes sous licence permissive (MIT, Apache 2.0, BSD)
# Aucune dépendance OpenAI / Anthropic / Google

# --- Inférence locale (Apple Silicon) ---
mlx>=0.16.0
mlx-lm>=0.16.0
mlx-embedding-models>=0.0.11

# --- API ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Modèles de données ---
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Base vectorielle ---
qdrant-client>=1.9.0

# --- File de messages / cache ---
redis>=5.0.0

# --- Base relationnelle (audit, Mac C) ---
asyncpg>=0.29.0

# --- Utilitaires ---
httpx>=0.27.0
python-dotenv>=1.0.0
structlog>=24.1.0
